[package]
name = "embed"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Real impl in Phase 3:
# SidecarEmbedder client — spawns Swift `mindforest-embed` (MLX-Swift),
# stdio newline-JSON protocol, exponential-backoff restart, Apple Silicon gate.
[dependencies]
domain = { path = "../domain" }
