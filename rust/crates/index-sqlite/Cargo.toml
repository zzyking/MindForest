[package]
name = "index-sqlite"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Derived FTS5 + (eventually) vector index over the markdown vault.
# Phase 1: nodes / links / nodes_fts (FTS5) / embed_jobs / meta tables;
# upsert / delete / search_fts / rebuild_from. search_vec returns
# EmbedUnavailable until Phase 3 wires sqlite-vec + the MLX sidecar.
[dependencies]
async-trait = { workspace = true }
blake3 = { workspace = true }
chrono = { workspace = true }
domain = { path = "../domain" }
rusqlite = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tokio-rusqlite = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
storage-fs = { path = "../storage-fs" }
tempfile = { workspace = true }
