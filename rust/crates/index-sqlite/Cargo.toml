[package]
name = "index-sqlite"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Real impl in Phase 1 task #1:
# - SqliteIndex: nodes / links / nodes_fts (FTS5) / embed_jobs / meta tables
# - upsert_node, delete_node, search_fts, rebuild_from(repo)
# - sqlite-vec virtual table reserved for Phase 3 (not loaded yet)
[dependencies]
domain = { path = "../domain" }
