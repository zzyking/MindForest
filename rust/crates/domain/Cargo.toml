[package]
name = "domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Real types (NodeId via ULID, Topic, Node, repository / indexer / embedder
# / agent traits, ForestError) land in Phase 1 task #11.
[dependencies]
