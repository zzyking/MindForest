[package]
name = "domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Pure types and trait definitions — no tokio runtime, no I/O.
# Implementations live in storage-fs / index-sqlite / embed / agent crates.
[dependencies]
async-trait = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
ulid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
