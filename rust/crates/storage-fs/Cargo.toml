[package]
name = "storage-fs"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Real impl in Phase 1 task #2:
# - FsRepository: atomic write (tempfile + rename), frontmatter parse via gray_matter
# - Watcher actor (notify, 200ms debounce, per-node single-flight mutex,
#   500ms mtime suppression window)
# - blake3 content_hash for incremental embedding
[dependencies]
domain = { path = "../domain" }
