[package]
name = "app-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Real impl in Phase 1 task #6:
# Composes domain + storage-fs + index-sqlite + (eventually) embed + agent
# into ForestService. apply_patch is the transactional boundary.
[dependencies]
domain = { path = "../domain" }
storage-fs = { path = "../storage-fs" }
index-sqlite = { path = "../index-sqlite" }
embed = { path = "../embed" }
