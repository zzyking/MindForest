[package]
name = "api"
version = "0.1.0"
edition = "2021"

# Phase 1 stub. Real impl (axum routes, FTS search, agent SSE) lands in tasks
# #6 / #8. Surface kept compatible so apps/desktop continues to build.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
