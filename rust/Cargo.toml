[workspace]
members = [
  "apps/api",
  "apps/desktop",
  "crates/domain",
  "crates/storage-fs",
  "crates/index-sqlite",
  "crates/embed",
  "crates/agent",
  "crates/app-core",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
authors = ["MindForest"]

# Shared dependency versions. Crates opt-in to specific features per-need.
[workspace.dependencies]
async-trait = "0.1"
axum = { version = "0.7", features = ["macros"] }
blake3 = "1"
bytemuck = "1.18"
chrono = { version = "0.4", features = ["serde"] }
governor = "0.7"
futures = "0.3"
# OS-native secret storage. `keyring` 3.x ships with no platform
# backend enabled by default (the "mock" mode silently keeps secrets
# in-process and loses them on restart — surprising default). We opt
# in to the native backends per OS: Security.framework on macOS,
# Credential Manager on Windows, sync D-Bus Secret Service on Linux
# (pure-Rust, doesn't drag in libsecret). `crypto-rust` is what the
# secret-service backend needs to encrypt session-bus messages.
keyring = { version = "3", default-features = false, features = [
  "apple-native",
  "windows-native",
  "sync-secret-service",
  "crypto-rust",
] }
notify = "6"
notify-debouncer-full = "0.3"
# Default-disabled rustls — we opt into the system's TLS via
# rustls-tls-native-roots so reqwest doesn't pull aws-lc-rs and friends
# (lighter binary, fewer transitive deps).
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls-native-roots", "stream", "json"] }
# `bundled` ships SQLite at a known version (avoids divergence with the
# system lib). sqlite-vec registers itself via sqlite3_auto_extension at
# process startup, so we don't need rusqlite's `load_extension` feature.
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlite-vec = "0.1"
thiserror = "1"
tokio = { version = "1", features = ["full"] }
tokio-rusqlite = "0.5"
tokio-stream = "0.1"
serde_yml = "0.0.12"
tempfile = "3"
tower-http = { version = "0.5", features = ["cors", "trace", "fs"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ulid = { version = "1", features = ["serde"] }
